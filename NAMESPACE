useDynLib(hypervolume)

export(hypervolume, plot.HypervolumeList, plot.Hypervolume, hypervolume_set, hypervolume_inclusion_test, summary.Hypervolume, summary.HypervolumeList, estimate_bandwidth, hypervolume_importance, nonconvexfeatures) 

exportClasses(
    "Hypervolume",
     "HypervolumeList" 
)

import(rgl)
import(geometry)
import(igraph)
import(MASS)
import(Rcpp)
import(methods)
